# Railway — web service (root directory). Worker üçün ayrıca service əlavə edin:
#   New → Empty Service → Root Directory: worker
[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/api/health"
restartPolicyType = "always"
