# ─── Netlify — Next.js ───
# Netlify UI-da env dəyişənlərini təyin edin (WORKER_API_URL, WORKER_API_TOKEN,
# ADMIN_USERNAME, ADMIN_PASSWORD). Firebase konfiqurasiyası defolt işləyir.
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
